import { CompareWithDateDirective } from './compare-with-date.directive';

describe('CompareWithDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareWithDateDirective();
    expect(directive).toBeTruthy();
  });
});
